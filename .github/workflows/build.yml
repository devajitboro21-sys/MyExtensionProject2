name: Build Extension
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build AIX
        uses: pavi2410/AIX-Action@v2
        with:
          name: abajaba

